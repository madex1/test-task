<div class="section" id="section-content">
    <div class="container">

        <div class="row">
            <div class="col-lg-8 mx-auto">

				<?= $variables['page']->content; ?>

            </div>
        </div>

    </div>
</div>

<section class="section bg-gray">
    <div class="container">
        <h2>Recent posts</h2>

        <div class="row gap-y">

			<?php
			$author = $variables['page']->author;
			$id = $variables['page']->getObjectId();

			$news = new selector('pages');
			$news->types('hierarchy-type')->name('news', 'item');

			$result = $news->result();

			$count = 0;
			foreach ($result as $item) {
				if (
					$item->author !== $author ||
					$item->getObjectId() === $id ||
					$count > 2
				) {
					continue;
				}
				$count++; ?>

                <div class="col-md-6 col-lg-4">
                    <div class="card d-block border hover-shadow-6 mb-6">
                        <a href="<?= $item->link ?>">
                            <img class="card-img-top" src="<?= $item->anons_pic ?>" alt="Card image cap">
                        </a>
                        <div class="p-6 text-center">
                            <p><a class="small-5 text-lighter text-uppercase ls-2 fw-400" href="#">
									<?= $item->publish_time->getFormattedDate('Y-m-d') ?>
                                </a></p>
                            <h5 class="mb-0"><a class="text-dark" href="#"><?= $item->h1 ?></a></h5>
                        </div>
                    </div>
                </div>

			<?php } ?>

        </div>
    </div>
</section>